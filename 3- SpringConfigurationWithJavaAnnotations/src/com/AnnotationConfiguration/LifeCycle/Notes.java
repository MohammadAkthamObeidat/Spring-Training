package com.AnnotationConfiguration.LifeCycle;

public class Notes {

    /**
     * # To configure the lifecycle methods fo a specific bean, we marks the methods with the following annotations:
     *
     * 1- @PostConstruct annotation it will be placed on the method that will do some job after create an instance of the bean.
     *      - So the above annotations used directly after the bean constructed and after the injection of the dependencies.
     *
     * 2- @PreDestroy annotation it will be placed on the method that will do some job before destroy the bean.
     */
}
